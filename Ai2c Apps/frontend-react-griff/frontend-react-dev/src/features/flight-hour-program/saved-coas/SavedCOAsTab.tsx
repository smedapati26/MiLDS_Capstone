import React from 'react';

import { Box } from '@mui/material';

/**
 * Saved COAs Tab
 * @returns React Node component
 */
const SavedCOAsTab: React.FC = (): React.ReactNode => {
  return <Box data-testid="fhp-saved-coas-tab">Saved COAs Tab</Box>;
};

export default SavedCOAsTab;
