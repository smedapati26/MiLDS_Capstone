import React from 'react';

import { Box } from '@mui/material';

/**
 * Calculator Tab
 * @returns React Node component
 */
const CalculatorTab: React.FC = (): React.ReactNode => {
  return <Box data-testid="fhp-calculator-tab">Calculator Tab</Box>;
};

export default CalculatorTab;
