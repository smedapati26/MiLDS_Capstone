import React from 'react';

import { Box } from '@mui/material';

const KitsCarousel: React.FC = (): React.ReactNode => {
  return <Box data-testid="kits-carousel">No Kits carousel</Box>;
};

export default KitsCarousel;
